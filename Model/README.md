## Model

The trained model binary is not stored in this repository.

To reproduce it, open `notebooks/` and run the training notebook
with Restart & Run All. The pipeline will export `titanic_model.joblib`
to this directory automatically.